#include <iostream>

using namespace std;
void areaperimeter(int,float *a,float *p);

int main()
{
    int radius;
    int x;
    float area,perimeter;
    cout<<"Enter radius";
    cin>>radius;
    areaperimeter(radius,area *,perimeter *);
    cout<<"area ="<<area;
    cout<<"Perimeter ="<<perimeter;
   cout<<x;

    return 0;
}
void areaperimeter(int,float *a,float *p)
{
    *a=3.14*r*r;
    *p=2*3.14*r;

}
